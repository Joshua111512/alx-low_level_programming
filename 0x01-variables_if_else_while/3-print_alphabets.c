#include <stdio.h>

/**
 * main - prints alphabets in upper and lowercase
 *
 * Return: 0
 */
int main(void)
{
	char letter;

	for (letter = 'A'; letter <= 'Z'; letter++)
		putchar(letter);

	putchar('\n')

		return (0);
}
