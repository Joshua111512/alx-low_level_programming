#include <stdio.h>

/**
 * main - print all last digit stored in the variable n by assigning
 * a random number to variable n each time it is executed
 * Return: 0
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n)
	}
	printf("\n");
	return (0);
}
